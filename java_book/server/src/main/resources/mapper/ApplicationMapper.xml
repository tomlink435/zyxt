<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gk.study.mapper.ApplicationMapper">
    <insert id="insert">
        insert into b_application(name, email, phone, address, description, project, courseName, classificationId)
        values(#{name}, #{email}, #{phone}, #{address}, #{description}, #{project}, #{courseName}, #{classificationId});
    </insert>
</mapper>
